rootProject.name = 'TestGradlePlugin'

buildCache {
    local {
        // set directory to the default location + a subdirectory
        directory = file("${rootProject.projectDir}/build-cache")
        removeUnusedEntriesAfterDays = 30
    }
}